
import React from 'react'
import styles from '../(components css)/blinker.module.css'

const Blink = () => {
  return (
    <h3 className={styles.blink} style={{fontWeight:500}}>Application for Faculty Position</h3>
  )
}

export default Blink